# GNN-Function-Approximator
A generative neural network designed to approximate the function  f(x) = (cos(x) +1) * 1/2  
Based on the ANN created in https://github.com/Yashaape/Handwriting_Classifier.git

